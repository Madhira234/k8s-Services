apiVersion: v1
kind: Service
metadata:
  name: payment-service 
spec:
  selector:
    ENV: DEV-PAYMENT
  type: NodePort
  ports:
    - port: 80
      protocol: TCP
      nodePort: 32000
      targetPort: 80
        